DECLARE a INTEGER,b INTEGER STREAM core0, 1 FILE 'datafile2.dat'
DECLARE c INTEGER,d INTEGER STREAM core1, 0.5 FILE 'datafile3.dat'

SELECT * STREAM str1 FROM core0#core1
SELECT * STREAM str2 FROM core0+core1
SELECT * STREAM str3 FROM core0-(1/2)
SELECT * STREAM str4 FROM core0&(1.5)
SELECT * STREAM str5 FROM core0%(4)
