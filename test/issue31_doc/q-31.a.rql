# Dokumentacja - wyjaśnienie zmiany nazwy pola

DECLARE a INTEGER STREAM core0, 0.1 FILE 'datafile1.txt'
DECLARE a BYTE STREAM core1, 0.2 FILE '/dev/urandom'

SELECT core0[0] + core1[0]/50 STREAM str1 FROM core0 + core1
