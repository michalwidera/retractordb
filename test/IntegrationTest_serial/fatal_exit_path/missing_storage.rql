# Katalog 'nosuchdir' celowo NIE istnieje: dyrektywa STORAGE go nie tworzy.
STORAGE 'nosuchdir'
SUBSTRAT 'memory'
DECLARE a INTEGER[3] STREAM src, 1 FILE 'data.txt'
SELECT MIN(a : 2) STREAM w FROM src
