# make install && ctest -R issue128_numeric_to_string -V

STORAGE 'temp'

DECLARE a INTEGER, b FLOAT STREAM src, 1/8 FILE 'data.txt' ONESHOT

SELECT to_string(src[0])+'_test', to_string(src[1]) STREAM dst FROM src
