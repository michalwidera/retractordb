# Generator strumieni: jedna linia zamiast czterech SELECT-ow.
# Rownowazny recznemu zapisowi w manual.rql — verify.sh porownuje wyniki co do bitu.
STORAGE '.'
DECLARE cell INTEGER[4] STREAM cells, 1 FILE 'cells.txt'
SELECT cells[$] STREAM cell[4] FROM cells
