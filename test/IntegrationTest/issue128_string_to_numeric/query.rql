# make install && ctest -R issue128_string_to_numeric -V

STORAGE 'temp'

DECLARE a STRING[16] STREAM src, 1/8 FILE 'data.txt' ONESHOT

SELECT to_integer(src[0]), to_float(src[0]), to_double(src[0]) STREAM dst FROM src
