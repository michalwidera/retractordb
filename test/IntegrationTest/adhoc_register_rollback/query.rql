# Plan jest tu tylko nosnikiem: badane jest wycofanie nieudanego importu ad-hoc
# i to, ze po nim serwer dalej liczy.

STORAGE 'temp'

DECLARE a INTEGER STREAM src, 1/8 FILE 'data.txt'

SELECT src[0] STREAM dst FROM src
