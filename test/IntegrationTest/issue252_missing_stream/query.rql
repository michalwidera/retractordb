# Plan jest tu tylko nosnikiem: badana jest odpowiedz serwera na nazwe, ktorej nie zna,
# i to, ze po niej dalej liczy.

STORAGE 'temp'

DECLARE a INTEGER STREAM src, 1/8 FILE 'data.txt'

SELECT src[0] STREAM dst FROM src
