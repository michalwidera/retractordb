[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "retractordb"
version = "0.1.0"
description = "Local stream monitoring through xqry"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["retractordb"]
