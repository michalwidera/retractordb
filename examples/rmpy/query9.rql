DECLARE a BYTE STREAM coreRnd, 0.1 FILE '/dev/urandom'

SELECT * STREAM str3 FROM coreRnd@(1,2)
SELECT str4[1]-str4[0] STREAM str4 FROM str3

SELECT * STREAM str5 FROM coreRnd@(1,10)
SELECT * STREAM str6 FROM str5.avg
