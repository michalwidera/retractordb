DECLARE a INTEGER STREAM core, 0.5 FILE 'datafile3.dat'

SELECT * STREAM agregate FROM core@(2,2)
SELECT * STREAM agreagteSerialize FROM core@(1,2)

SELECT * STREAM agregateMirror FROM core@(2,-2)
SELECT * STREAM aggreageSelective FROM core@(3,-2)
